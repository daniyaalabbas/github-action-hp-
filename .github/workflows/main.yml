name: CI/CD DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Test Application
        run: |
          python -c "import app; print('Application Loaded Successfully')"

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image
        run: |
          docker build -t daniyaalabbas/my-k8s-web:latest .

      - name: Push Docker Image
        run: |
          docker push daniyaalabbas/my-k8s-web:latest

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f deployment.yml
          kubectl rollout restart deployment my-k8s-web
          kubectl rollout status deployment my-k8s-web
